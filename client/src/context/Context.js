import { createContext } from "react";

/** @PhotosContext used to pass photo data */
export const PhotosContext = createContext();
export const UserContext = createContext();

// export default (PhotosContext, UserContext);
